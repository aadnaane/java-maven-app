pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo ("building the app")
      }
    }
    tage("test") {
      steps {
        echo ("testing the app")
      }
    }
    tage("deploy") {
      steps {
        echo ("deploying the app")
      }
    }
    tage("publish") {
      steps {
        echo ("publishing the app")
      }
    }
  }
}
